# Node modules
node_modules/

# Build output (regenerated by `npm run build`)
dist/
build/
www/
.output/
*.tgz

# Secrets — NEVER commit (PUBLIC repo). Only *.example templates are tracked.
.env
.env.*
!.env.example
*.jks
*.keystore
key.properties
keystore.properties
*.p8
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist
.npmrc.local

# Native build artifacts (generated)
android/.gradle/
android/build/
android/app/build/
android/.cxx/
android/local.properties
android/captures/
ios/Pods/
ios/build/
ios/DerivedData/
Pods/

# IDE / OS
.idea/
.vscode/
*.iml
*.swp
*.swo
*~
.DS_Store

# Logs / caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tmp
*.temp
.cache/
coverage/
.nyc_output/

# Lockfiles: yarn only
package-lock.json

# Custom (project convention)
*.ignore.*
project-record-ignore/

# Yarn 4 (Berry) — ignore cache/state, allow patches/plugins/releases/sdks/versions
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
